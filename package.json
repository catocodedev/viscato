{
  "name": "viscato",
  "version": "1.0.0",
  "description": "A new markdown language for the Web.",
  "main": "init.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node init.js"
  },
  "author": "catocodedev",
  "license": "MIT",
  "dependencies": {
    "properties-reader": "^2.2.0"
  }
}